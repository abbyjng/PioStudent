# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PioStudent' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PioStudent
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  pod 'Firebase/Storage'
  pod 'Firebase/Database'
  pod 'FirebaseUI'

  target 'PioStudentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PioStudentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

